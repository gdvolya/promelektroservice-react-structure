{
  "version": 2,

  // 🔁 Перенаправление всех маршрутов на SPA
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],

  // 📦 Заголовки кеширования
  "headers": [
    {
      // 🖼️ Кешируем ассеты (JS, CSS, изображения) на год
      "source": "/(.*\\.(js|css|woff2?|ttf|eot|svg|png|jpg|jpeg|gif|webp|ico|json|map))",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    },
    {
      // 🔄 Все остальные запросы — без кеша (HTML, API и т.п.)
      "source": "/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "no-cache"
        }
      ]
    }
  ],

  // ⚙️ Настройки сборки
  "build": {
    "env": {
      "NPM_FLAGS": "--legacy-peer-deps"
    }
  }
}
